---
- name: zip config
  archive:
    path: "{{ output_dir }}"
    dest: "{{ output_dir }}.zip"
    format: zip

- name: Delete config folder
  file:
    state: absent
    path: "{{ output_dir }}/"
  run_once: true